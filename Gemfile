# frozen_string_literal: true
# A sample Gemfile
# source "https://rubygems.org"
#
# hint - line below is taken from jekyll docs as a preferred GitPages handling way with bundler; not sure if ' vs " problem didn't occure when copying the text
# source 'https://rubygems.org' require 'json' require 'open-uri' versions = JSON.parse(open('https://pages.github.com/versions.json').read)
# gem 'github-pages', versions['github-pages']

# shorter version

source 'https://rubygems.org'
gem 'github-pages'


